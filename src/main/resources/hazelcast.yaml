hazelcast:
  map:
    default:
      time-to-live-seconds: 120
      max-idle-seconds: 120
      eviction:
        eviction-policy: LRU
        max-size-policy: PER_NODE
        size: 20000
    hrEmployeeCache:
      time-to-live-seconds: 60